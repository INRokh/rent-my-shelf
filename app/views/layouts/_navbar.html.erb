<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <a class="navbar-brand" href="#">Rent my Shelf</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <% if user_signed_in? %>
                    <%= link_to "Spaces", {controller: "spaces", action: "index"} , class: "nav-item nav-link" %>
                    <%= link_to "Campaigns", {controller: "campaigns", action: "index"} , class: "nav-item nav-link" %>
                <% end %>
            </div>
        </div>
        <% if user_signed_in? %>
            <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "nav-item nav-link" %>
        <% else %>
            <%= link_to "Login", new_user_session_path, class: "nav-item nav-link" %>
            <%= link_to "Sign Up", new_user_registration_path, class: "nav-item nav-link" %>
        <% end %>
    </div>
</nav>